??? summary "<font color="red">ebreak</font>  (Environment Breakpoint). <font color="green">rv_i</font> "

    RaiseException(Breakpoint).

    Make a request of the debugger by raising a Breakpoint exception.

??? summary "<font color="red">ecall</font>  (Environment Call). <font color="green">rv_i</font> "

    RaiseException(EnvironmentCall).

    Make a request of the execution environment by raising an Environment Call exception.
